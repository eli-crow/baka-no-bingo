{
  "name": "baka-no-bingo-server",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "start": "echo \"Error: not ready for production\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js,.json --ext",
    "lint:fix": "eslint . --ext .js,.json --fix"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.18.1",
    "socket.io": "^4.5.1"
  }
}
