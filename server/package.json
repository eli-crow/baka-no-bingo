{
  "name": "baka-no-bingo-server",
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "start": "echo \"Error: not ready for production\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.1.3"
  }
}